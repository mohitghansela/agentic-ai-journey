# My Understanding of Tokenization

Tokenization is important because LLMs don't understand text directly.

They convert text into tokens (numbers),
and all processing happens on these tokens.

Example:
"Hello AI" → [token IDs]
